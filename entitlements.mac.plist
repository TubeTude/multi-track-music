<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Required for Electron's V8 JIT compiler -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <!-- Required for Electron's dynamic code generation (WebAssembly, etc.) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- Allow loading unsigned plugins (needed by some Electron internals) -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- Camera access for recording tracks -->
  <key>com.apple.security.device.camera</key>
  <true/>
  <!-- Microphone / audio interface access -->
  <key>com.apple.security.device.audio-input</key>
  <true/>
</dict>
</plist>
